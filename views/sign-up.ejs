<h2 class="signup-heading">Create an Account</h2>

<p class="signup-intro">
  Join <strong>The Inner Circle</strong> and become part of a vibrant, exclusive community. 
  Gain access to member-only content, post and interact with others, and unlock hidden features.
</p>

<section class="signup-info">
  <h3>✨ Why Sign Up?</h3>
  <ul class="signup-benefits">
    <li><i class="fas fa-check"></i> Post and reply to messages</li>
    <li><i class="fas fa-check"></i> Access exclusive member and admin features</li>
    <li><i class="fas fa-check"></i> Get updates and notifications</li>
    <li><i class="fas fa-check"></i> Personalize your experience</li>
  </ul>
</section>

<form action="/sign-up" method="POST" class="signup-form">
  <div class="form-group">
    <label for="username">Username:</label>
    <input 
      type="text" 
      id="username" 
      name="username" 
      placeholder="Choose a username" 
      value="<%= typeof body !== 'undefined' ? body.username : '' %>"
      required 
    />
  </div>

  <div class="form-group">
    <label for="first_name">First Name:</label>
    <input 
      type="text" 
      id="first_name" 
      name="first_name" 
      placeholder="Your first name" 
      value="<%= typeof body !== 'undefined' ? body.first_name : '' %>"
      required 
    />
  </div>

  <div class="form-group">
    <label for="last_name">Last Name:</label>
    <input 
      type="text" 
      id="last_name" 
      name="last_name" 
      placeholder="Your last name" 
      value="<%= typeof body !== 'undefined' ? body.last_name : '' %>"
      required 
    />
  </div>

  <div class="form-group">
    <label for="email">Email:</label>
    <input 
      type="email" 
      id="email" 
      name="email" 
      placeholder="Enter your email" 
      value="<%= typeof body !== 'undefined' ? body.email : '' %>"
      required 
    />
  </div>

  <div class="form-group">
    <label for="password">Password:</label>
    <input 
      type="password" 
      id="password" 
      name="password" 
      placeholder="Create a password" 
      required 
    />
  </div>

  <div class="form-group">
    <label for="confirm_password">Confirm Password:</label>
    <input 
      type="password" 
      id="confirm_password" 
      name="confirm_password" 
      placeholder="Re-enter your password" 
      required 
    />
  </div>

  <button type="submit" class="signup-submit">Sign Up</button>
</form>

<% if (messages.error?.length) { %>
  <div class="flash error"><%= messages.error.join("<br>") %></div>
<% } %>
<% if (messages.success?.length) { %>
  <div class="flash success"><%= messages.success.join("<br>") %></div>
<% } %>

<hr class="signup-divider" />

<p class="signup-login-link">
  Already have an account? <a href="/log-in">Log in here</a>.
</p>

